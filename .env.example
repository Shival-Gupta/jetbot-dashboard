# .env
# Environment for Flask (development or production)
FLASK_ENV=development
# FLASK_ENV=production

# Secret key for Flask sessions and security.
# Generate a strong random key and paste it here.
# Example generation: 
# openssl rand -hex 32
# python3 -c 'import secrets; print(secrets.token_hex(24))'
SECRET_KEY=YOUR_SUPER_SECRET_KEY

# Optional: Define the entry point for Flask commands (if used)
# FLASK_APP=main.py